<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.attendance">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UpdateAttendanceActivity"></activity>
        <activity android:name=".UpdateLectureActivity" />
        <activity android:name=".DisplayLecturesActivity" />
        <activity android:name=".SubjectCRUDActivity" />
        <activity android:name=".DisplayAttendanceActivity" />
        <activity android:name=".MySubjectActivity" />
        <activity android:name=".MarkAttendanceActivity" />
        <activity android:name=".AdminAddStudent" />
        <activity android:name=".CreateAccountActivity" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>